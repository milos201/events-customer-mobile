# AGENTS

## Navigation

- Prefer real back navigation whenever the user is returning to the previous screen.
- Do not keep pushing duplicate routes onto the stack for actions like "Back to discovery" or "Back to shop".
- Use stack back behavior whenever possible, and only push or replace when the user is intentionally moving to a new destination.
